#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Fast gate: catches the mistakes worth catching at commit time without
# making you wait. The build moved to pre-push — see .husky/pre-push.
echo "▸ typecheck"
npm run typecheck || exit 1

echo "▸ tests"
npm test -- --silent || exit 1
