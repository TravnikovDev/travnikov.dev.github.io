// Shared types for components
export interface NavItem {
  label: string;
  path: string;
  color?: string;
}

export interface SocialLinkItem {
  name: string;
  url: string;
  icon: string;
  hoverColor: string;
}